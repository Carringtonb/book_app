<!DOCTYPE html>
<html lang="en">
<head>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title id="detailsPage">More Details</title>
    <link href="/styles/reset.css" rel="stylesheet" type="text/css" />
    <link href="/styles/base.css" rel="stylesheet" type="text/css" />
    <link href="/styles/layout.css" rel="stylesheet" type="text/css" />
</head>
<body>
    <ul>
      <%  database.forEach(book => { %>
            <li class="details"><%= book.title %></li>
            <li class="details"><%= book.author %></li>
            <li class="details"><%= book.description %></li>
            <img class="details" src="<%= book.image %>" />
        </ul>
        
        <form action="/update/<%=book.id %> ?_method=PUT" method="POST">
            <input type="text" name="title" value="<%- book.title %>">
            <input type="text" name="author" value="<%- book.author %>">
            <input type="text" name="description" value="<%- book.description %>">
            <button>Update</button>
        </form>
            <% }) %>


</body>
</html>